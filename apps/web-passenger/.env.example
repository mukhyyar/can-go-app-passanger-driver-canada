# Web passenger — copy to `.env.local` as needed
NEXT_PUBLIC_CANGO_API_BASE=http://127.0.0.1:4000/api

# Optional: real Google / Apple client IDs (otherwise local mock via API when NODE_ENV=local)
# NEXT_PUBLIC_GOOGLE_CLIENT_ID is not required — client ID comes from GET /auth/oauth/config
